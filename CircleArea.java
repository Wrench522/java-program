public class Area {
    public static void main(String[] args) {
      int radius = 20;

        // Prompt the user to enter the radius
        // Calculate the area
        double area = Math.PI * radius * radius;

        // Display the area
        System.out.println(radius + area + "cm2");
    }
}